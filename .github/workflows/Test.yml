name: Test

on:
  push:
    branches:
      - main
    tags: ["*"]
  workflow_dispatch:

jobs:
  test:
    uses: ./.github/workflows/ReusableTest.yml
    with:
      os: ${{ matrix.os }}
      version: ${{ matrix.version }}
      arch: ${{ matrix.arch }}
      allow_failure: ${{ matrix.allow_failure }}
      run_codecov: ${{ matrix.version == '1.12' && matrix.os == 'ubuntu-latest' }}
    secrets:
      codecov_token: ${{ secrets.CODECOV_TOKEN }}
    strategy:
      fail-fast: false
      matrix:
        version:
          - "lts"
          - "1.12"
        os:
          - ubuntu-latest
        arch:
          - x64
        allow_failure: [false]
        include:
          # Latest Julia pre-release (currently 1.13 rc): tested in a temp env
          # with the `julia-1.13-compat` JET fork (the only JET that supports
          # 1.13); see ReusableTest.yml. Allowed to fail.
          - version: "pre"
            os: ubuntu-latest
            arch: x64
            allow_failure: true
